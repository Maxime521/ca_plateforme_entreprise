# NPM configuration
audit-level=high
fund=false

# Override audit for Firebase's undici dependency (moderate severity, external)
# These are Firebase SDK internal dependencies that cannot be patched by us
# Monitor for updates: https://github.com/firebase/firebase-js-sdk/issues